## Step 7: ROS2 RL dry-run validation

### Purpose

Validate the Jetson ROS2 RL pipeline without STM32 hardware.

### Verified

- ONNX backend loaded `models/exp043_policy.onnx`
- `/cmd_vel` is reflected in observation indices 6~8
- fake joint/IMU feedback is received
- `feedback_age_ms` stays low
- `raw_action[12]` is generated by ONNX Runtime
- `target_rad = ik_ref + raw_action * 0.25`
- loop runs at 50Hz

### Test commands

```bash
ros2 launch rl_locomotion rl_locomotion.launch.py
ros2 topic pub --once /cmd_vel geometry_msgs/msg/Twist \
"{linear: {x: 0.05, y: 0.0, z: 0.0}, angular: {x: 0.0, y: 0.0, z: 0.0}}"
ros2 topic echo /control/rl/debug --once